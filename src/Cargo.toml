[package]
name = "bq268-edl-diag"
version = "0.1.0"
edition = "2021"

[lib]
path = "rust/lib.rs"
crate-type = ["staticlib"]

[profile.release]
opt-level = "z"
lto = true
panic = "abort"
codegen-units = 1
